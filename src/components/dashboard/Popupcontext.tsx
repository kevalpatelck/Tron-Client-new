// import { createContext, useContext } from 'react';
// import SliderImage from '../SliderImage';

//  const Popupcontext=createContext<boolean>();

//  export usepopup=()=>{ useContext(Popupcontext)}